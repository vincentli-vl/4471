/*
    * TODO:
    * - Create a table component (let it have as many cells as needed from props)
    * - Let the number of columns/cells be dynamic
    * - Use react hooks to fetch data from the backend then display it in the table
*/

export default function Table() {
  return <div>Table</div>;
}